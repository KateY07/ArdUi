# 第一轮性能优化实验：2026-09-19

本轮是隔离验证，未把实验改动合入正式 ARD/ArdUi，未发布或部署。完成 16 组短测，以及加密、完整性、切换和断线恢复回归。结论：64 KiB 搬运缓冲值得继续；减少过早重发的机制有效；其余项目尚无稳定的性能收益证据。持续 TCP 满载时的 UDP 超时仍是待解决问题。

## 测量范围与限制

- 两端、目录和 ArdRelay 均在本机。测量期间业务保持 IPv4 Direct，并核对业务 tunnel/PID；没有把辅助探测连接当成业务直连。
- **这些 Gbps 数字是同机处理能力，不是公网速度。** TCP/UDP 业务入口为回环地址，但 Iroh 实际选路在各组之间不同：有 `127.0.0.1`，也有本机的 `172.20.10.7` 和 `192.168.6.1`。不能把整批统称为固定 loopback 路径。
- 各组内没有业务路径切换，组间地址、MTU 和运行时状态却不完全相同。同一个未优化 C# 基线从 1052/954 降至 439/444 Mbps，因此单次百分比不代表稳定的因果收益；也未定位这次波动的全部原因。
- 每方向发送 8 秒，计时包括接收确认和排空；以接收确认的业务字节计量，而非发送缓存接受量。吞吐阶段确认字节数量，随机内容完整性另由功能回归验证。
- 空载 TCP 64 B、UDP 64/1200 B 各 1000 次，预热 50 次。这是应用 echo RTT，不是 TCP SYN RTT，也不是单独测量 RDP。
- 满载项另做一次 8 秒 TCP 下载，并串行测量 1200 B UDP echo，回复后等待 10 ms，每次超时 2 秒。它不是固定频率采样；超时会压低后续采样数，不能把超时比例直接称为网络丢包率。
- CPU 是两端测试程序及 ARD 子进程之和，排除目录和 Relay；应按实际接收 GiB 归一化。分配量包括测试器，不能当作单个 ArdUi 客户端的专属成本。
- 本机无可用的真实网络延迟模拟设施，当前也没有用户此前使用的公网 IPv6 对照条件。未验证高 RTT 窗口收益，未外推公网提速或公网流量节省比例。

## ARD 搬运缓冲

仅在实验副本中将 TCP↔QUIC 搬运替换为可选容量的 BufReader + copy_buf；0 表示保留原 tokio::io::copy，8 表示相同容量的 API 对照。读取不会等待填满缓冲，原有 FIN/半关闭逻辑保持。

| 批次 / 方案 | 实际直连地址 | 上传 Mbps | 下载 Mbps | TCP RTT p50 ms |
|---|---|---:|---:|---:|
| 首批 / 原逻辑 | 127.0.0.1 | 1281.0 | 1205.5 | 0.1591 |
| 首批 / 64 KiB | 172.20.10.7 | 1584.6 | 1493.4 | 0.1535 |
| 首批 / 128 KiB | 127.0.0.1 | 1654.6 | 1490.8 | 0.1572 |
| 复测 / 64 KiB | 127.0.0.1 | 745.3 | 1067.2 | 0.1757 |
| 复测 / 8 KiB API 对照 | 192.168.6.1 | 634.1 | 717.4 | 0.3554 |
| 复测 / 原逻辑 | 127.0.0.1 | 742.4 | 692.9 | 0.2346 |

首批 64 KiB 的上传/下载约 +24%；复测上传基本持平，下载约 +54%。这些只是本次各组差值，**不是已验证的固定加速比例**。下载两批都正向，值得下一步固定选路并交错重复；128 KiB 暂无值得为其增加内存的稳定额外收益证据。

纯 ARD 首批总 CPU 约为原逻辑 17.80、64 KiB 15.02 核秒/GiB；复测分别为 45.01、27.95。跨批成本变化也说明不能只依据一组数字推广。

## ArdUi 单项与组合

实验变体：aes 复用方向独立的 AES-GCM 对象并在既有发送锁内加密；packed 使用临时池化缓冲合并长度头和密文写入；resend 要求帧龄及 ACK 停滞达到门槛再重发，切换/恢复路径仍立即重发；combined 合并这三项。udp 仅将实验双方的分片上限从 1100 改成 1280，未实现生产所需的路径 MTU 自适应。

下表 p95 **仅包括成功的满载 UDP 回复**；超时必须同时阅读。

| 方案 | 上传 Mbps | 下载 Mbps | 满载 UDP 成功 p95 ms | 2 秒超时 / 尝试 | 发起端重发帧累计 |
|---|---:|---:|---:|---:|---:|
| 原正式源码 | 1039.3 | 1137.0 | 13.64 | 3/188 | 378 |
| 实验基线 | 1051.9 | 954.0 | 8.83 | 3/189 | 437 |
| aes | 1107.2 | 956.5 | 9.64 | 3/159 | 448 |
| packed | 1100.9 | 1043.2 | 8.64 | 3/93 | 449 |
| resend | 1097.3 | 975.8 | 16.73 | 4/89 | 0 |
| combined | 800.1 | 689.3 | 32.16 | 4/41 | 0 |
| combined 复测 | 804.6 | 541.2 | 23.94 | 4/63 | 0 |
| 基线复测 | 438.8 | 444.3 | 42.76 | 4/28 | 516 |
| udp | 505.4 | 370.1 | 39.46 | 4/42 | 385 |
| 基线 C# + ARD 64 KiB | 533.0 | 574.1 | 15.41 | 0/305 | 400 |

- **AES / 合并写入：未确认稳定提速。** 首批几个百分点的差异小于基线波动。C# 每接收有效载荷字节的分配仍约 6.21–6.29 B，没有证明总分配显著下降。
- **重发策略：机制目标达到，但吞吐收益未确定。** 已测正常路径的冗余重发从数百帧降为 0；切换和断连恢复功能通过。没有抓取链路总字节，因此不能据此计算公网省流量比例。
- **组合版：暂不采用。** 首批吞吐低于基线，复测又高于当时显著变慢的基线，不能断言回退幅度完全由组合代码造成，更不能宣称提速。首批单位数据总 CPU 从基线 50.81 增至组合 60.53 核秒/GiB，原始 CPU 总量下降只是少传了数据。
- **UDP 分片：功能可通，性能收益未确认。** 在当次足够的 datagram 容量下，1200 B 业务数据不再需要两个 overlay 分片；空载 1000 次收全，但满载仍有 4 次超时。不能直接把固定 1280 应用于所有公网/中继路径。
- **满载 UDP 超时是重复出现的实际现象。** 原版、单项和组合均复现。某次 64 KiB 全链路 0/305 不足以证明修复。双方诊断未记录 overlay UdpDrops、认证失败或重放拒绝，仍不能排除更底层排队、调度、GC 或丢弃；本轮未定位到唯一根因。

## 功能验证

- 并发加密压力：6144 个双向 TCP/UDP 加密帧，内容完整、nonce 唯一且连续；3072 次 UDP 重放被拒绝。
- 内部 transit 回归：随机完整性、10 MiB 超过 8 MiB 窗口持续传输、原 TCP socket 切换与回退、UDP 分片和去重、空闲 UDP 映射恢复、TCP 半关闭、诊断脱敏通过。
- 实际 ARD 集成：64 KiB ARD + combined C#，通过身份登记和授权、端到端握手、TCP 与 RDP 大小的原生 UDP、独立 Python C 的两条 P2P 腿、同一业务连接切换到 C；杀死 C 后原业务 TCP/UDP 回退且 1 MiB 内容完整。
- 杀死基础 ARD 后，同一监听端口及原业务 socket 自动恢复，本次用时 **16.9 秒**。这是功能恢复通过，不代表无感恢复或更快恢复；授权撤销后业务及候选路径销毁也通过。
- 未运行 Windows SMB 或真实远程桌面画面测试；上述 UDP 通过不能替代 mstsc 显示 UDP 的最终验证。

## 产物与后续优先级

1. 优先定位 TCP 满载时 UDP 超时，并把固定速率探针与排队/丢弃证据补齐。
2. 固定 Iroh 业务选路后交错复测原版与 64 KiB；通过后再决定是否合入。
3. 保留按 ACK 进度抑制过早重发的候选，继续测高延迟和切换场景；AES、packed、组合版暂不合入。
4. 高 RTT 窗口、真实公网 IPv6、并发业务队头阻塞仍待独立条件验证，本轮未完成，未宣称收益。

原始记录：[首批 JSON](dist/optimization-round1/runs.json)、[确认批 JSON](dist/optimization-round1/confirm.json)。每项记录包含独立目录、选路日志、时间、二进制/源码 hash；确认批还含双方 overlay 快照和诊断 ZIP。

实际集成日志：[client.log](dist/v2-integration-20260919-095739/client.log)。

可重现入口：`tests/optimization/prepare_ard.py`、`prepare_ui.py`、`run_round1.py`（含 `--confirm`），以及 `tests/performance_fixture.py`；方法见 `tests/performance/README.md`。

收尾校验 24 个正式文件的 SHA-256 与实验前相同，含 ARD 主源码、清单、锁文件、锁定 README、正式 ARD 二进制和所链接的 ArdUi 源码。仅新增/调整测试、实验副本和本报告，未更新正式程序、用户配置或线上服务。
