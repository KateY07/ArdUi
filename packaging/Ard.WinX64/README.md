# KateY07.Ard.WinX64

This package contains the authenticated Windows x64 `ard.exe` runtime and its SHA-256 checksum.

It is versioned independently so ArdUi upgrades can reuse the unchanged ARD binary. ARD source code is not included.
