# KateY07.FRD.WinX64

This package contains the FRD Windows x64 runtime archive, its archive checksum, and the checksum of every contained file.

It is versioned independently so ArdUi upgrades can reuse the unchanged FRD runtime.
