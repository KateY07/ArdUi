# v2.pre1 验证记录

本地回归日期：2026-09-17；NJ 部署和公网校验日期：2026-09-18。客户端仍为 .NET 8 框架依赖、Avalonia Simple、单 C# 文件；没有修改 ARD 引擎或其锁定 README。

## 成品与存档

- 实现前源码归档：`dist/archives/before-v2.pre1-20260917-114825.zip`，SHA-256 `a32e5077100f24cfbeb786dfd2b1acd85ff39d635ae3b94885caf9aab6bbc84a`。
- 最终客户端：`dist/final-v2.pre1/ArdUi.exe`，25,292,585 字节，SHA-256 `758c20c3350bb4c47a041428fbe4432328091aabad01ac09a06e5ab6c1b074e4`。
- ARD 未变化：`ard 2.0.0-pre.6`，SHA-256 `04ebed96baecc2fd5b67318b1d02742f777b0351c84ee5b1c1b163a05dc98b5d`。
- NJ 变更压缩包：`dist/upload-v2.pre1.zip`，10,018,599 字节，SHA-256 `caa326eaf43193de42130049d837e3654a4dae6964173311b195ccb9b2642020`。只含变更文件，不含 ARD、用户数据、测试身份或发布私钥。
- 慢速上传改用 `dist/upload-v2.pre1-delta.zip`：252,822 字节，SHA-256 `764bdcf2bfd3b7f33c407f7e44fe8388555097a37a00e952ddf3ba60c9a869ff`。以 NJ 已有 pre11 的 SHA-256 `36e05397db04283a84c5939c11a4e6466de2cbafeec60c77c745eb1e7805f7ae` 为基准；只复制相同数据块并补入变化内容，重建后再次核对完整成品哈希。该部署差量不改变客户端成品或安装方式。

## 已通过

| 检查 | 结果与边界 |
| --- | --- |
| 编译与发布 | .NET 编译 0 警告、0 错误，框架依赖单 EXE；对发布后的 EXE 运行以下回归。 |
| Avalonia 无头与身份 | 界面渲染、首次创建、重复安装保留、损坏身份拒绝覆盖。 |
| 目录服务 14 项用例 | 候选租约、签名/重放、角色权限、容量、双端续租、密钥并发创建、缺少 B 本次批准/替换 B 临时身份/批准过期均拒绝。 |
| 加密与数据报 | 双向独立密钥、篡改和错误密钥拒绝、UDP 重放拒绝、分片后原生 UDP 回显、闲置映射释放与重新建立。 |
| 连续 TCP | 10 MiB 数据通过 8 MiB 有界窗口，内容完整；修复了接收循环同步回写 ACK 导致的双向阻塞。 |
| 本地路径切换 | 同一业务 TCP 连接从基础路径到 C、基础路径恢复、杀死 C 后回退；UDP 映射保留，重复报文不重复交付。 |
| 真实 ARD 三节点 | 两个真实 C# 客户端、独立 Python C、本地 ArdRelay 和目录服务；两段 Direct 日志确认，TCP 与 RDP 大小的原生 UDP 实际通过 C。 |
| C 故障 | 终止 C 及其 ARD 子进程，原 TCP 套接字回退基础路径后继续传输完整 1 MiB，UDP 继续工作。 |
| 基础进程故障 | 终止基础 ARD，保留内层会话和业务套接字；最终成品本轮约 16.8 秒恢复，同一本地端口。另一次约 21.8 秒；不是保证恢复时长。 |
| 授权全流程 | 首次确认前不持久授权；获准后传输；无需首次密码重连；撤销拒绝；连续三轮添加、传输、移除、重新添加；关闭被控终止会话。 |
| 安装与修复 | 在隔离目录实际执行 install.ps1，校验通过的当前/历史缓存不下载；重复安装保留身份、机器码/关闭状态文件与设备列表；损坏 UI 时只获取 UI、复用 ARD。下载阶段以测试函数提供已校验成品，公网下载另作部署检查。 |
| 诊断导出 | 客户端及 C 均产生 ZIP；包含路径、质量、故障时间线、资源与计数，不包含能力令牌或业务载荷；IP 默认脱敏。 |
| NJ 部署与公网检查 | 服务健康接口返回 v2.pre1；HTTPS `/ardui`、安装脚本和节点 ZIP 的内容/哈希与成品一致，EXE 可访问；实际签名注册、票据公钥固定、候选心跳通过。测试节点声明无剩余容量，不承载其他用户流量。 |

真实 ARD 测试日志：`dist/v2-integration-20260917-123852/client.log`；授权流程：`dist/v2-integration-20260917-123932/client.log`；最终安装测试目录：`dist/install-v2-96cc05c9f1c9448cb7c35eea00590a80`。

NJ 在应用差量前校验旧版和差量包哈希，重建后核验最终 EXE。旧服务、安装页面与 SQLite 在线备份保存于 `/var/backups/ardui-before-v2.pre1-20260918T070557Z`；8080/8090 监听保持正常。只推送 GitHub 源码，没有上传 GitHub Release。

本地目录服务为隔离的 loopback HTTP，生产配置仍强制可信 HTTPS；不得把测试中的旧日志标签“HTTPS registration”作为本地 TLS 测试证据。localhost A–B 本身更快，因此三节点回归显式选择 C 来验证传输与切换；生产选择器仍拒绝用 C 替换已建立的 A–B Direct。质量阈值另有确定性用例。

## 静态检查与仍待验证的范围

已检查新协议的身份绑定、审批、nonce 空间、重放窗口、可靠流顺序/ACK、资源上限、退出清理和诊断排除项；`git diff --check` 通过。Bandit 在当前运行时未安装，不能声称已通过该扫描或独立安全审计。

未据此次本地回归宣称以下项目完成：

- 不同 NAT、动态公网 IPv4、公网 IPv6 的实际网络覆盖与跨地区质量收益。
- 多个真实候选竞争、满载/恶劣网络下的长期稳定性和吞吐压测；8 次采样、256 KiB 探测仅是首版粗估策略。
- Windows mstsc 窗口实际显示 UDP 传输。原生 UDP 测试不能代替 Windows RDP 服务与客户端的协商证据。
- Windows SMB 实测，按用户要求本轮暂不进行。
- 程序退出、全部路径长时间不可达、业务自身超时或授权撤销时无中断；本版不作这种保证。

普通 ArdUi 客户端未自动成为中继。独立 C 需自愿启动，按需登记；客户端自动贡献仅记录后续路线。

## 复现

```powershell
./build.ps1 -ArdPath ./tools/ard.exe -Python python
python ./tests/package_v2.py
./tests/install_v2.ps1
```

构建回归需本地 `../target/release/ard-relay.exe`，测试使用临时目录和本地 Relay，不改 NJ 服务。安装回归不创建开机快捷方式、不启动用户界面、不触碰用户正式安装目录。
