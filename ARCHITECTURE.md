# ArdUi 源码结构

从 v2.pre2 开始按职责拆分 C# 源码；保持同一程序集、文件作用域命名空间 `ArdUi`、Avalonia SimpleTheme 和纯代码界面。交付的 ArdUi 仍是依赖 .NET 8 的单 EXE；源码文件数量不影响交付方式。

| 目录/文件 | 职责 |
| --- | --- |
| `ArdUi.cs` | 启动入口与命令分派；保留版本常量供发布工具使用。 |
| `GlobalUsings.cs` | 共享导入及 Avalonia 类型别名。 |
| `App/` | 应用初始化、紧凑主窗口、状态刷新与用户操作。 |
| `Core/` | 配置、设备状态、永久身份、设备目录及授权流程、会话生命周期、诊断与发布校验。 |
| `Transport/` | ARD 子进程管理、受限本地网关、TCP/UDP 转发和 SMB 集成。 |
| `Overlay/` | 内层端到端加密、可靠流复用、原生 UDP、路径迁移与恢复。 |
| `TransitClient/` | 候选节点协调、测量、选择及回退；与 Python `transit/` 区分。 |
| `Frd/` | FRD 子进程生命周期、临时会话，以及 `--port` 的透明 TCP/UDP 转发。 |
| `Testing/` | 保留原无头、自检、授权和三节点回归入口，增加真实 FRD 回归。 |
| `tests/frd-protocol/` | 引用生产转发文件的独立测试；传输任意二进制数据，不解析 FRD 协议或操作桌面。 |
| `tests/frd-readiness/` | 引用生产监听检查，验证子进程拥有同端口 TCP/UDP、静默启动、取消与错误退出。 |
| `server/` | Python 设备目录与 NJ 部署工具。 |
| `transit/` | 独立 ArdTransit 节点，与桌面客户端分开交付。 |

FRD 启动请求只在已授权的内层会话内处理。被控端直接授权 FRD `--port` 上的回环 TCP/UDP；主控端使用同一个本地端口双向透明转发。ArdUi 不读取 FRD 握手、不改写业务字段、不按业务包格式或长度筛选。UDP 保持数据报边界和来源映射，仍受现有隧道 MTU 限制；会话结束时移除临时端口权限并关闭子进程。

同一个 FRD 转发入口允许 8 条独立 TCP 连接，各自保留字节流顺序与半关闭行为；这不是“一个端口只能建立一条 TCP 连接”。超过资源上限时仅拒绝新连接并记录原因。UDP 业务载荷上限为 1482 字节。启动就绪通过 Windows 监听表确认该 FRD 进程拥有指定 `127.0.0.1:port` 的 TCP 和 UDP，持续读取日志仅用于诊断；不再依赖 `Remote listener ready:` 等文案，也不发送 FRD 业务探测包。

FRD 自行负责认证、业务协议和单端口内复用。本次适配采用开发组正式交付的 v1.pre13 二进制；该版本所有业务经 `--port` TCP/UDP 传输，单会话最多 4 条 TCP 连接、UDP 数据报最大 1172 字节。v1.pre12 不满足该接口，不与新版适配组合发布。只通过公开 CLI 和黑盒联调集成，不读取、编译或修改 FRD 源码。

新功能应放入对应职责文件。更改传输或生命周期后运行对应回归；构建项目显式列出生产目录，避免将 `dist` 或测试替身编译进客户端。
