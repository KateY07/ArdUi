# ArdUi 增强中继畅想（暂不实现）

> 状态：设计畅想，暂不实现，不属于当前 ArdUi 交付契约。

## 目标

当 A 与 B 无法 P2P 直连、只能通过 NJ ArdRelay 通信时，由 NJ 发现并选择网络条件良好的 ArdUi 节点 C，分别引导 A–C 与 B–C 建立 P2P 直连。两条直连均成功后，C 承载 A–B 的加密流量，减少 NJ 的带宽压力并改善延迟。

C 不需要静态公网 IP。它只需保持在线、向 NJ 登记最新候选地址，并能够同时与 A、B 打洞直连。若任一段仍使用 ArdRelay，则 C 不应成为该会话的转发节点。

```text
建立与协调：A ── NJ ArdRelay ── B
                       │
                       └── 发现并协调 C

业务路径：  A ══ P2P ══ C ══ P2P ══ B
             └──── A–B 内层端到端加密 ────┘

C 失效：    A ── NJ ArdRelay ── B
```

## 实现边界

该方案不修改 `ard.exe` 或 `ard-relay.exe`。现有 ARD 继续负责：

- ArdRelay 引导与 QAD 公网映射探测；
- LAN、IPv4、IPv6及端口映射候选发现；
- EndpointId 鉴权；
- A–C、B–C 的打洞与 P2P 直连；
- TCP/UDP 传输和网络路径诊断。

功能改动集中在：

- `ArdUi/prototype.py`：内层端到端加密、线路选择、C 的盲转发、心跳和诊断；
- `ArdUi/server/arduiserver.py`：转发候选注册、健康状态、候选选择及短期票据；
- `ArdTransit`：最终作为独立应用交付；最小原型可先由 `prototype.py transit` 独立进程承担。

## 加密边界

经过 C 时存在两类加密：

```text
内层：ArdUi A ↔ ArdUi B
作用：真正的 A–B 端到端加密，C 无法解密业务内容。

外层：ARD A ↔ C、ARD C ↔ B
作用：逐段身份认证、机密性、完整性和防注入。
```

C 会终止两条外层 ARD 连接，但只得到 A–B 内层密文。它可以观察 EndpointId、连接时间、流量大小和方向，不能读取 RDP、SMB 或 ArdUi 控制内容，也不能冒充 B。

基础网络在安全模型中可以被视为不可信。理论上，正确的内层协议足以在明文基础网络上保护业务；实现上仍保留 ARD/Iroh 固有的 QUIC 加密，因为它同时保护心跳、票据、诊断和逐段身份，并避免重新实现可靠传输、防注入和拥塞控制。

## 临时身份与会话密钥

临时密钥不能只由 NJ 指定。每个临时公钥必须由设备永久身份签名，握手至少绑定：

```text
A 永久 EndpointId
B 永久 EndpointId
A、B 临时 X25519 公钥
sessionId
协议版本
签发时间与过期时间
双方随机数
双方永久身份签名
```

A、B 使用临时 X25519 共享秘密和完整握手摘要，通过 HKDF 派生内层会话密钥。TCP 数据采用带长度、序列号的 AEAD 帧；UDP 数据报采用独立序列号和重放窗口。可使用现有 `cryptography` 依赖提供的 X25519、HKDF 和 ChaCha20-Poly1305，无需增加 Python 依赖。

即使 NJ 被攻破并选择恶意 C 或篡改临时公钥，永久身份签名和本地保存的 EndpointId 仍能阻止中间人解密或冒充。被攻破的服务器或 C 仍可拒绝服务、消耗带宽或收集流量元数据。

## 转发票据与权限

NJ 为每个转发会话签发短期票据：

```text
sessionId
A EndpointId
B EndpointId
C EndpointId
签发时间
过期时间
最大带宽
服务器签名
```

C 仅在票据中的 A、B 同时通过身份认证后建立转发映射。C 不接受任意目标 IP、域名或端口，只能在票据指定的两个 EndpointId 之间转发不透明密文，因此不能成为公共出口代理。

C 必须实施最大并发数、单会话带宽、总带宽、数据报尺寸、速率、半连接超时和空闲超时限制。贡献转发带宽必须由设备所有者明确开启。

## 心跳、路径选择与回退

C 定期向 NJ 发送签名心跳，报告：

- 版本、在线时间和 EndpointId；
- NAT/QAD 结果和候选地址类型；
- 当前并发、上下行速率及配置限额；
- C–NJ RTT、抖动和丢包；
- CPU、内存及最近错误摘要。

C–NJ RTT 只能用于初筛。NJ 选出少量候选后，A、B 必须实际建立 A–C、B–C 连接并测量两段路径。预计经 C 的往返延迟约为：

```text
RTT(A,C) + RTT(C,B) + C 的处理开销
```

只有两段均为 Direct，且估算结果明显优于 NJ ArdRelay 时才切换到 C。路径优先级为：

```text
A–B Direct
    ↓ 不可用
A–C–B ArdTransit
    ↓ 不可用
NJ ArdRelay
```

C 心跳超时、任一 P2P 段断开、负载超限或路径恶化时，ArdUi 应停止使用 C 并回退 NJ。最小原型允许当前 RDP/SMB 连接重建；若要求已有会话无感迁移，ArdUi 覆盖层还需实现序列确认、缓存、恢复或内层 QUIC，复杂度会显著增加。

## 诊断包

诊断包包含：

- ArdUi、ARD 和协议版本；
- 本机 EndpointId；
- NAT/QAD 探测结果和候选地址类型；
- NJ、A、B、C 各段路径类型、RTT、抖动和丢包；
- 收发字节数、并发量和限流状态；
- 路径建立、选择、迁移、回退的时间线；
- 最近错误和资源使用量。

IP 地址默认脱敏。诊断包不得包含私钥、密码、会话密钥、完整票据或业务载荷。

## 最小原型规模

最小原型暂不追求 RDP 会话无感迁移，预计改动为：

| 部分 | 预计新增代码 |
|---|---:|
| 内层握手、密钥派生和加密帧 | 100–150 行 |
| C 双向盲转发 | 100–150 行 |
| 路径选择、心跳和 RTT | 100–160 行 |
| TCP/UDP 本地入口接入 | 100–160 行 |
| NJ 注册、候选查询和票据 | 80–140 行 |
| 三节点、安全及断线回归 | 150–250 行 |

生产逻辑预计新增约 480–760 行 Python，回归测试约 150–250 行。原型应至少覆盖：强制 A–B 直连失败、A–C 与 B–C 直连成功、C 无法解密内层数据、篡改身份签名被拒绝、重放数据报被拒绝、C 断线后回退 NJ，以及 TCP 与 UDP 业务流量测试。
