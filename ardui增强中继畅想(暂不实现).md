# ArdUi 增强中继方案 — v2.pre1

> 2026-09-17：用户授权实现。保留原文件名供历史链接使用，状态已从畅想变为 v2.pre1 实现方案。
> 实现前源码存档：`dist/archives/before-v2.pre1-20260917-114825.zip`，SHA-256 `a32e5077100f24cfbeb786dfd2b1acd85ff39d635ae3b94885caf9aab6bbc84a`。

## 目标和职责

A 为主控，B 为被控，C 为独立运行的 ArdTransit 转发程序。NJ 维护候选列表、健康租约和短期转发票据；A 负责发起探测和最终选路，B 验证并配合，两端使用同一个 C。C 不需要静态公网 IP；良好 NAT、动态公网 IPv4 或 IPv6 均可，但必须实测 A–C 和 B–C 同时为 P2P Direct，不能仅凭网络类型或 C 的自报判断。

路径优先级：A–B Direct → 符合条件且质量更好的 A–C–B → NJ ArdRelay。初始连接仍通过既有 ARD 建立并完成身份核对、首次同意或永久授权检查。随后才探测 C，未经授权的设备不能借助 C 访问业务端口。

```text
NJ：候选注册 / 心跳 / 签名票据 / 会话协调
A ===== ARD Direct ===== C ===== ARD Direct ===== B
  └──────── A–B 内层端到端加密、稳定业务会话 ───────┘
A ================= 原有 ARD 连接 ================= B
                    保持备用，故障回退
```

`ard.exe` 和 `ard-relay` 的协议和锁定 README 不修改。客户端仍为单个 `ArdUi.cs`、Avalonia Simple、.NET 8 框架依赖发布；新增独立的 Python `transit/ardtransit.py` 供 Windows/Linux 节点运行，复用 ARD 进程完成打洞、身份认证与 TCP/UDP 传输。中央协调服务仍使用 Python。

## 身份和内层加密

沿用永久 Ed25519 身份与已保存的双方授权。A、B 的临时 ARD 身份已有永久密钥签名，内层临时 P-256 ECDH 握手只在该已认证且已获授权的 ARD 连接内交换，绑定覆盖层会话编号、双方临时公钥和授权能力。HKDF-SHA256 派生双向独立 AES-256-GCM 密钥，TCP/UDP 分离 nonce 空间，序号禁止复用。握手和密码不经过 C，也不交给 NJ。

C 终止两段外层 ARD 加密，只转发内层密文；它不能读取业务数据或伪造有效帧。NJ/C 被攻破仍可拒绝服务或观察流量元数据。UDP 校验认证标签并检查重放窗口；TCP 以会话序号去重、有序交付。随机会话、密钥只在进程内存保存，不写入诊断包。

NJ 以持久 Ed25519 服务密钥签发短期票据，绑定 A/B/C 永久身份、双方本次 ARD 临时身份、路由编号、有效期和限额。C 的临时 ARD 公钥由 C 永久身份签名。各端通过可信 HTTPS 获取并固定 NJ 票据公钥；密钥变更拒绝自动接受。

每次申请还需 B 在当前已授权会话内重新签署短期批准，绑定本次 A/B/C、路由编号和双方临时身份。仅持有旧永久授权的 A 不能自行替换 B 的临时公钥或在 B 未参与时申请转发。关闭被控或撤销授权后，B 不再签署这种批准。

## 候选、心跳和自动切换

C 明确启用后注册、定期签名心跳，报告容量、当前会话、流量、C–NJ 请求 RTT、资源使用和最近错误。NJ 清理过期候选，最多返回三个候选；负载信息用于初筛，不能证明直连或实际业务质量。

A/B 为候选建立独立 ARD 连接，经原有加密控制路径报告各自的 Direct 状态；还通过 C 测量端到端 UDP RTT、丢包、抖动和受限带宽探测。任何一段退回 Relay，立即取消 C 的业务资格。原路径保留，按 RTT、可用带宽和滞回选择，避免周期性反复切换。A–B 后续达成 Direct 时优先回切。

v2.pre1 的具体策略：

- C 每 3 秒心跳；超过 20 秒不更新则不进入候选列表。A/B 分别续租，任意一方租约过期即关闭路由；默认租约 90 秒。
- NJ 按占用率初筛，最多返回 3 个候选。A/B 的建链和 UDP 探测可并行；同一主控会话的带宽突发探测串行，避免互相干扰。最多等待约 85 秒确认双段 Direct，业务继续使用原路径。
- 每条路径先预热，再测 8 次 UDP RTT；统计中位数、P90、P90 减中位数的抖动估计以及丢包。用 256 KiB 有界加密回显粗估 TCP 有效吞吐，不宣称是链路容量或 RDP 的可用带宽保证。
- 候选至少成功 7/8 次且吞吐大于零；丢包不得超过原路径与 12.5% 两者中的较大者，吞吐不得低于原路径的 70%。评分为 `P90(ms) + 2 × 抖动(ms) + 1000 × 丢包率`，低者优先。
- 只有评分改善至少 `max(5 ms, 15%)`，或吞吐提高至少 50% 且 P90 退化不超过 5 ms、丢包不增加时才切换。若原路径探测基本失效，可选择满足准入要求的候选。首版的 8 次采样较粗，后续应按实际诊断数据校准，不能据此声称精确测得低丢包率。
- 正常重新评估间隔至少 60 秒；C 失联或一段失去 Direct 时回退。质量连续三轮恶化才触发质量回退，避免偶发抖动造成反复切换；故障后可提前重新寻找候选。A–B 已直连时不再引入 C。

## 客户端自动贡献的路线评估

每个 ArdUi 都可以具备成为 C 的能力，但本版只交付独立 ArdTransit；尚未默认让用户电脑替其他设备转发。后续建议增加独立的、自愿开启的“允许帮助转发”选项，与“允许被控”相互独立。开启后自动注册、保持心跳、接受按需探测；无需用户填写 IP、端口或判断 NAT 类型。

候选资格属于特定 A/B 会话：能帮 A/B，不代表能帮所有设备。中央服务只维护轻量目录和初筛信息，按需下发少量候选；不建立所有节点两两常驻连接，也不把所有客户端启动时的网络探测结果当成资格证明。获得双方授权后的业务先走可用路径，候选探测在后台进行。

面向普通客户端的默认值应比专用节点保守：低并发、明确上行限额、计量网络和电池状态控制、优先本机业务、可随时退出。中继只能配对被授权会话的密文，不能成为任意目标的开放代理。端到端加密保护内容，不能消除带宽、电量、CPU、元数据暴露和被滥用消耗资源的成本。

最佳落地顺序为：独立 C 与可复现的切换回归 → 少量自有长期在线节点试用并校准策略 → 将同一转发角色集成进 ArdUi，提供自愿自动贡献。它对经常无法 A–B 直连、距离 NJ 远的用户有价值；对稳定直连的用户没有必要增加中转。IPv6、良好 NAT、动态 IPv4 都只是潜力信号，最终仍以双段实测和端到端收益为准。

## TCP、UDP 与 RDP

TCP 使用覆盖层流编号、顺序号、累计确认和有界重传缓存。切换只替换传输路径，保留本地及 B 端业务 TCP 套接字；缺失帧在新路径重传，重复帧不重复写入业务。缓存满时背压，全部路径暂时中断时保留会话并限时等待恢复。

UDP 继续使用 ARD 原生数据报，不伪装为 TCP。覆盖层分片使原来的 RDP UDP 报文不因额外加密头超过 ARD MTU；重组有大小、并发与超时上限。切换时保留 UDP 流映射，允许 UDP 原生语义下的丢包和乱序。TCP/UDP 本地端口保持一致，继续支持 RDP UDP 协商。

“尽量不中断”适用于双方进程与业务套接字仍存活、缓存和恢复窗口内仍有可用路径的情况；不能保证全网长时间中断、程序退出、业务自身超时或授权撤销时继续会话。授权撤销和关闭被控优先，立即销毁全部路径和业务流。

## 限额与诊断包

C 不接受任意出口地址，只能配对票据绑定的两端。实施并发、带宽、帧/数据报大小、半连接和空闲超时限制。票据用于短期建链，已建立路由有独立租约续期与撤销机制。

客户端提供 ZIP 诊断导出，C 提供同等命令行导出：版本、会话/路径类型、两段 Direct 判定、候选评分、UDP RTT/抖动/丢包、预计带宽、收发字节、重传/去重/重放拒绝、缓存使用、资源使用、切换与故障时间线。ARD 输出保留并纳入有界诊断缓存；IP 默认脱敏。不包含私钥、首次密码、能力令牌、完整票据、密钥、原始握手和业务载荷。

## 验收

- C 注册、租约超时、票据签名/角色/重放/权限拒绝。
- A/B/C 真正运行 ARD，两段 Direct 后才可自动选 C；任一段 Relay 不允许选用。
- TCP 与 UDP 经 C 双向传输；UDP 分片、重排、重复和篡改测试。
- 正在传输的同一 TCP 连接在原路径、C、回退路径之间切换，数据完整且无重复。
- 杀死 C、断开某一段、原路径恢复、授权撤销、关闭被控、资源限额与退出清理。
- 保留 v1 身份、机器码、授权与安装缓存行为；Avalonia 无头回归。
- RDP UDP 的实际协商取决于两端 Windows/RDP 策略和环境；自动化 UDP 服务测试与实际 mstsc UDP 证据分别报告，不混同。

实际完成状态和测试证据另记于 `V2-VALIDATION.md`，未验证场景不得标记为已通过。
